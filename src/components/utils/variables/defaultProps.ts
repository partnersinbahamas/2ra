import TDefaultProps from '../types/props/TDefaultProps';

const defaultProps: TDefaultProps = {
  stile: 'default',
  size: 'medium',
  status: 'default',
  anchorOrigin: {
    vertical: 'bottom',
    horizontal: 'right',
  },
};

export default defaultProps;
